# Falsification

- Fails if the published demo replaces the Stitch environment with a static screenshot-only wrapper.
- Fails if the Korean sample data or required sections are missing from the primary screen.
