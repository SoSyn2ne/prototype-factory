# Falsification - Cluster Tree Explorer

Reject or redesign this direction if:

- Operators do not find prototypes faster than with the existing force map.
- The tree becomes a static file browser without useful PF-specific grouping.
- A new dependency adds too much bundle or maintenance cost for a sidebar-level feature.
- Search/filter is weaker than the existing cluster board interactions.
