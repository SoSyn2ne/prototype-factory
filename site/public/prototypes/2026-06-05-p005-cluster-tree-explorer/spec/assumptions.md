# Assumptions - Cluster Tree Explorer

- This is a smoke-test prototype, not a committed decision to adopt the Trees package.
- The first implementation should be a secondary tree/list mode, not a replacement for the existing force-map cluster board.
- Package adoption still needs dependency, bundle, and Next.js compatibility review.
