# Assumptions
- KNOWN: S1 documents exposed replay links; profile is `operator-dense`.
- ASSUMPTION: operators will paste URLs if the scan is simulated/local and the receipt is redacted.
- UNKNOWN: each vendor's permission semantics and revoke API availability.
